###########################################
# Mail service SMTP configuration
# Copy this to `.env` next to docker-compose.yml
###########################################

###########################################
# Dev (MailHog - no real emails)
###########################################
# SMTP_HOST=mailhog
# SMTP_PORT=1025
# SMTP_SECURE=false
# SMTP_REQUIRE_TLS=false
# SMTP_TLS_REJECT_UNAUTHORIZED=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=no-reply@helpdesk.local

###########################################
# Prod/real sending (Gmail example)
# - Enable 2FA on the Gmail account
# - Create an App Password (16 chars) and use it as SMTP_PASS
# - SMTP_USER should be the Gmail address
###########################################
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_REQUIRE_TLS=true
SMTP_TLS_REJECT_UNAUTHORIZED=true
SMTP_USER=your@email.com
SMTP_PASS=your_pass
SMTP_FROM=your@email.com

###########################################
# Common
###########################################
PORT=3000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://70.153.24.245,http://70.153.24.245:3000
NEXT_PUBLIC_AUTH_API_URL=http://70.153.24.245/api/auth
